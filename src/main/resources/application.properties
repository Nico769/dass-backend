# See here
# https://www.baeldung.com/spring-boot-h2-database
# The following settings are only needed to force H2 with Hibernate.
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Enable H2 web console
#spring.h2.console.enabled=true

# Let Jackson parse and accept enums (stored as strings)
# while ignoring case sensitivity
spring.jackson.mapper.accept-case-insensitive-enums=true
# Uncomment for additional debug info. Useful for checking HTTP methods invocation
# debug=true
